package javaTester;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

public class Topic_04_Scope {

    //Các biến được khai báo trong 1 hàm / block code: Gọi là biến cục bộ (Local)

    // Nếu trong 1 hàm có 2 biến cùng tên (Global/local) thì nó sẽ lấy  biến local dùng

    // TH biến local khai báo mà chưa khởi tạo gán giá trị vào thì nó sẽ báo lỗi

    //// Nếu trong 1 hàm có 2 biến cùng tên (Global/local) mà muốn lấy biến global thì dùng từ khóa this

    //driver.get(this.homepageURL);



}
